<UserControl x:Class="Readify.Pages.MainMenu.BookPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Readify.Pages.MainMenu"
             mc:Ignorable="d" 
             d:DesignHeight="1318" d:DesignWidth="1090">
    <Grid>
        <Border Margin="0 0">
            <StackPanel>
                <TextBlock Text="Чистый код" FontFamily="{StaticResource Inter}"
                                      FontWeight="Bold" FontSize="30" Margin="0 0 0 10"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="2.5*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Padding="0 0 10 0" Grid.Column="0" >
                        <StackPanel>
                            <!-- Изображение книги -->
                            <Border Height="350" Background="#F5F5F5">
                                <Image Margin="0 0 0 0" Source="{Binding LastBook.ImageBytes, Converter={StaticResource ImageConverter}}"
                                                    Stretch="UniformToFill"  RenderOptions.BitmapScalingMode="HighQuality"
                                                    VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            </Border>
                            <Button Content="Читать" Style="{StaticResource BookReadButton}" 
                                    Command="{Binding EditUserCommand}"
                                    Visibility="{Binding IsEditButtonVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            <Button Content="Удалить из библиотеки" Style="{StaticResource BookButton}" 
                                    Command="{Binding EditUserCommand}"
                                    Visibility="{Binding IsEditButtonVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            <Button Content="Написать отзыв" Style="{StaticResource BookButton}" 
                                    Command="{Binding EditUserCommand}"
                                    Visibility="{Binding IsEditButtonVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            <Button Content="Удалить отзыв" Style="{StaticResource BookButton}" 
                                    Command="{Binding EditUserCommand}"
                                    Visibility="{Binding IsEditButtonVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            <Button Content="Удалить с сервиса" Style="{StaticResource DeleteBookButton}" 
                                    Command="{Binding EditUserCommand}"
                                    Visibility="{Binding IsEditButtonVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>
                        </StackPanel>
                    </Border>
                    <Border Grid.Column="1" Padding="5 0">
                        <StackPanel>
                            <TextBlock Text="Опубликовал: admin" FontFamily="{StaticResource Inter}" FontSize="23"
                                       FontWeight="Bold"/>

                            <TextBlock Text="Жанр: программирование" FontFamily="{StaticResource Inter}" FontSize="21"
                                       FontWeight="Bold" Margin="0 5" TextWrapping="Wrap"/>
                            <TextBlock Text="Плохой код может работать, но он будет мешать развитию проекта и компании-разработчика, требуя дополнительные ресурсы на поддержку и «укрощение».

Каким же должен быть код? Эта книга полна реальных примеров, позволяющих взглянуть на код с различных направлений: сверху вниз, снизу вверх и даже изнутри. Вы узнаете много нового о коде. Более того, научитесь отличать хороший код от плохого, узнаете, как писать хороший код и как преобразовать плохой код в хороший.

Книга состоит из трех частей. Сначала вы познакомитесь с принципами, паттернами и приемами написания чистого кода. Затем приступите к практическим сценариям с нарастающей сложностью — упражнениям по чистке кода или преобразованию проблемного кода в менее проблемный. И только после этого перейдете к самому важному — концентрированному выражению сути этой книги — набору эвристических правил и «запахов кода». Именно эта база знаний описывает путь мышления в процессе чтения, написания и чистки кода. " 
                                       FontSize="16" FontFamily="{StaticResource Inter}" FontWeight="Regular"
                                       Foreground="#5A5A5A" TextWrapping="Wrap"/>

                            <Border Background="#5A5A5A" Height="0.1" Margin="0 20 0 10"/>

                            <TextBlock Text="Отзывы:" FontFamily="{StaticResource Inter}" FontSize="23"
                                       FontWeight="Bold"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="2" Padding="10 0 0 0">
                        <StackPanel>
                            <DockPanel VerticalAlignment="Center" HorizontalAlignment="Right">
                                <TextBlock Text="5/5" FontFamily="{StaticResource Inter}" 
                                           FontSize="23" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0 0 5 0"/>
                                <Path Fill="#FEC810"
                                                Data="M8 .25a.75.75 0 0 1 .673.418l1.882 3.815 4.21.612a.75.75 0 0 1 .416 1.279l-3.046 2.97.719 4.192a.751.751 0 0 1-1.088.791L8 12.347l-3.766 1.98a.75.75 0 0 1-1.088-.79l.72-4.194L.818 6.374a.75.75 0 0 1 .416-1.28l4.21-.611L7.327.668A.75.75 0 0 1 8 .25Zm0 2.445L6.615 5.5a.75.75 0 0 1-.564.41l-3.097.45 2.24 2.184a.75.75 0 0 1 .216.664l-.528 3.084 2.769-1.456a.75.75 0 0 1 .698 0l2.77 1.456-.53-3.084a.75.75 0 0 1 .216-.664l2.24-2.183-3.096-.45a.75.75 0 0 1-.564-.41L8 2.694Z"
                                                Stretch="Uniform" Margin="0 0 0 1" Height="25" Width="26"/>
                            </DockPanel>
                            <TextBlock Text="Другие книги жанра" Width="265" FontFamily="{StaticResource Inter}"
                                       FontWeight="Bold" FontSize="16" Margin="0 8 0 8"/>
                            <Border  Height="50" CornerRadius="8" Padding="5 0"
                                            Cursor="Hand">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#F5F5F5"/>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#EFEFEF"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="Программирование"
                                                   FontSize="21"
                                                   FontFamily="{StaticResource Inter}"
                                                   FontWeight="Medium"
                                                   VerticalAlignment="Center"
                                                   HorizontalAlignment="Center"/>
                            </Border>

                            <TextBlock Text="Продолжить чтение" Width="265" FontFamily="{StaticResource Inter}"
                                       FontWeight="Bold" FontSize="16" Margin="0 8 0 8"/>
                            <Border Margin="0 0 0 0" Height="270" Cursor="Hand"
                                   CornerRadius="8" >
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <!-- Стандартный фон -->
                                        <Setter Property="Background" Value="#FFFFFF"/>
                                        <!-- Триггер при наведении -->
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#EFEFEF"/>
                                                <!-- Замените на нужный цвет -->
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>

                                <StackPanel HorizontalAlignment="Center" Width="200">

                                    <!-- Изображение книги -->
                                    <Border Height="204" Width="145">
                                        <Image Margin="0 20 0 0" Source="{Binding LastBook.ImageBytes, Converter={StaticResource ImageConverter}}"
                                                    Stretch="UniformToFill"  RenderOptions.BitmapScalingMode="HighQuality"
                                                    VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                    </Border>

                                    <!-- Название книги -->
                                    <TextBlock Text="{Binding LastBook.Name}" Width="145"
                                                                FontFamily="{StaticResource Inter}"
                                                                TextAlignment="Center" FontSize="20"
                                                                Margin="0 5 0 2" FontWeight="SemiBold"/>

                                    <!-- Страница -->
                                    <TextBlock Text="{Binding LastBookPageText}" MaxWidth="145" HorizontalAlignment="Center"
                                                                FontFamily="{StaticResource Inter}" FontWeight="Regular"
                                                                TextAlignment="Center" FontSize="16">
                                    </TextBlock>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Border>

                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
