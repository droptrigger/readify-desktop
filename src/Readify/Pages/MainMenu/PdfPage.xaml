<UserControl x:Class="Readify.Pages.MainMenu.PdfPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Readify.Pages.MainMenu"
             mc:Ignorable="d" 
             xmlns:main="clr-namespace:Readify.ViewModels.MainMenu"
             d:DataContext="{d:DesignInstance Type=main:PDFViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Изображение страницы -->
        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <Image Source="{Binding CurrentPageImage}" Stretch="None"/>
        </ScrollViewer>

        <!-- Панель навигации -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="Предыдущая" Command="{Binding PreviousPageCommand}" 
                    Width="100" Margin="5" Padding="10,5"/>
            <TextBlock Text="{Binding PageNumberDisplay}" VerticalAlignment="Center" 
                       Margin="10,0" FontSize="14"/>
            <Button Content="Следующая" Command="{Binding NextPageCommand}" 
                    Width="100" Margin="5" Padding="10,5"/>
        </StackPanel>
    </Grid>
</UserControl>
